# TDD Guard Configuration Wizard - Core Dependencies
# Python 3.8+ required

# Testing framework
pytest>=7.0.0
pytest-cov>=4.0.0

# TDD Guard integration
tdd-guard-pytest>=0.1.2

# Rich terminal UI
rich>=13.7.0

# Standard library modules (included with Python)
# - pathlib (Python 3.4+)
# - argparse (Python 3.2+)
# - json (built-in)
# - typing (Python 3.5+)

# Development dependencies (optional)
pytest-flask>=1.3.0  # For Flask integration testing if needed